# AppDataHub

Windows 应用数据多账号切换工具，基于 Rust + Tauri 2 构建。

通过 JSON 配置文件定义目标应用的路径，支持一键切换账号配置。

## 构建

```bash
npm install
npm run dev      # 开发
npm run build    # 生产构建
```

## 技术栈

Rust + Tauri 2 / 原生 HTML/CSS/JS / serde / MIT
